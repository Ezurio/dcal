This example is a simple library that exercises various session functions and implements common parameter handling for the various examples apps.  It is used by sample apps to have a unified parameter set.

If additional parameter handling is desired, this code can be used as an example on parameter handing.
